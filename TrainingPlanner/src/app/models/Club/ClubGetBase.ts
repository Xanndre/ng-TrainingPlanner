import { Picture } from '../Stuff/Picture';

export class ClubGetBase {
  id: number;
  name: string;
  city: string;
  phoneNumber: string;
  email: string;
  picture: Picture;
  isFavourite: boolean;
}
